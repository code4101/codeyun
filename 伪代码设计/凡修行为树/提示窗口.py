"""

通用函数基础设施：
匹配当前帧场景(帧)
"""

for 帧 in "弹窗"分组下的每一帧:
    if not 匹配当前帧场景(帧):
        continue

    if 帧 == #47:  # 如果匹配的是#47
        pass
    else:
        帧.点击([空白] or [关闭] or [确认])  # 如果该帧没有[空白]，继续尝试找点击[关闭], [确认]

    yield
