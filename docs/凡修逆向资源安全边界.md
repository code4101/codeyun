# 凡修逆向资源安全边界

## 固定外部目录

凡修逆向原始资源、解包产物、解析导出和相关文档统一放在仓库外：

```text
D:\home\chenkunze\data\m2606凡修逆向
```

CodeYun 仓库只保存源码、测试、轻量文档和必要配置，不保存 APK 解包后的大体积资源包、图片资源原件、临时抓包片段或一次性探针产物。

## 默认路径

后端默认使用以下目录：

```text
FANXIU_RESOURCE_ROOT        = D:\home\chenkunze\data\m2606凡修逆向\frxx_game_files
FANXIU_RESOURCE_EXPORT_ROOT = D:\home\chenkunze\data\m2606凡修逆向\frxx_analysis_exports
```

对应实现见 `backend/core/fanxiu/catalog/resources.py`。如需临时切换，使用环境变量 `FANXIU_RESOURCE_ROOT` / `FANXIU_RESOURCE_EXPORT_ROOT`，不要把路径写进前端或业务页面。

## 仓库内允许保存什么

允许保存：

- 解析、展示、校验这些资源的源码。
- 小型测试样本和单元测试数据。
- 文档化结论、流程、字段语义和验收脚本。
- 从资源二次加工后写入 CodeYun 数据库的轻量结构化结果。

不允许保存：

- APK 原包、Unity bundle 原件、大批量图片、音频、模型等逆向原始资产。
- 临时截图、OCR 裁剪、抓包片段、探针 JSON、一次性 stdout/stderr。
- 为了前端显示而复制一份图片到仓库。

## 前端图鉴的资源使用原则

凡修图鉴前端通过后端接口读取本地逆向资源，例如：

```text
/api/fanxiu/resources/icon?name=...
```

图片资源不需要复制进 CodeYun 数据库。数据库只保存邮件、附件、道具 ID、图标名、名称、类型、品质等结构化字段；实际图片仍由后端从 `m2606凡修逆向` 目录解析并返回。

## 验收口径

资源链路不能只看“数据库有字段”。至少要同时验证：

- 当前资源根和导出根必须先通过 `verify_fanxiu_reverse_boundary.py`。它检查后端默认根、环境变量覆盖、关键资源/导出产物、文件数量，以及 `frxx_game_f` 这类疑似误名同级目录；虚拟机 `D:\TapTap\Support\android_emulator\engine\vms\...` 路径不能作为当前默认根。
- 关键逆向输入和导出产物必须有机器可读 manifest。`verify_fanxiu_reverse_manifest.py` 会记录 `raw_inputs`、`filelist.csv/filelistVersion`、关键 rows/catalog、icon/by_source/apk_static_index 等条目的大小、mtime、SHA256 或目录摘要，并写入 `frxx_analysis_exports/parsed_configs/reverse_manifest_audit/`。
- 数据库邮件附件存在 `item_id`、可读名称和 `icon/small_icon`。
- 道具配置里的图标必须先通过底层资源导出检查。`verify_fanxiu_item_icons.py` 会遍历完整本地 `item_catalog`，确认每个唯一 icon 都能从逆向资源导出；这是资源恢复层检查，不能用页面首屏或 HTTP 抽样替代。
- 图标接口能返回真实图片。`verify_fanxiu_wiki_icons.py --full-items` 必须全量扫描道具目录，并分页扫描功法、灵界、数字门角色、斗破角色等必需图标目录；活动目录允许空 icon，默认只做页面级抽样。
- 图鉴接口不能只验证状态码和数量。`verify_fanxiu_wiki_endpoints.py` 必须检查核心列表行的关键字段，例如图鉴链接索引的 `tab/id/title/kind`，功法/道具的 `id/name/icon`，活动的 `id/name/source_table/presence_status`，以及视觉、音频、协议等资源的定位字段。
- 功法、道具、活动、灵界、数字门角色、斗破角色这些主图鉴卡片目录必须保持可加载；列表要非空，关键 ID/标题字段完整，默认每类按首段/中段/尾段抽 8 条代表性样本。除活动这类本身常见空图标的目录外，当前展示用的小图标字段必须能通过 `/api/fanxiu/resources/icon` 返回真实图片；有详情接口的目录还必须能打开对应详情。
- 前端邮件图鉴能渲染附件图片。
- 附件链接能进入道具详情页。
- 独立资源详情页 `/fanxiu-resource/:resourceType/:resourceId` 必须保持可用；当前已支持并验收 `gongfa/item/activity/digitdoor/doupotd/lingjie` 六类，默认每类打开 5 条真实资源详情页，必须能渲染标题、ID、返回图鉴链接和正文。除活动这类本身常见空图标的资源外，必须加载真实图标，且页面图片 URL 要和后端列表返回的 `icon/small_icon` 名称一致。
- 独立资源详情页不能为了富文本链接无条件拉完整 `/api/fanxiu/resources/wiki/link-index`。详情页应按当前资源正文、效果、附件等文本请求轻量 `/api/fanxiu/resources/wiki/link-targets`，只取当前文本实际出现的可跳转目标；全量 link-index 只保留给完整图鉴页或索引构建场景。
- 典型正文 fallback 可读，例如“灵脉收益”“修炼值自动领取通知”。
- 道具图鉴分类筛选后，列表行必须渲染真实游戏图标，不能只露出单字 fallback。
- 道具图鉴分类筛选后的浏览器验收必须随滚动多批采样。只验证筛选后的首屏不够，因为用户反馈的风险来自“随便一翻”后仍可能出现缺图或错图。
- 道具图鉴分类筛选必须能由 URL 直接复现。`/fanxiu/wiki?tab=item&type_key=...&sub_type_key=...` 进入页面时，地址栏、激活筛选、首屏行、后端请求和图标都必须落在同一分类下；不能依赖 localStorage 里的旧筛选或用户手点。
- 道具图鉴无筛选 URL 必须能清空旧筛选。先打开带分类筛选的 URL，再打开 `/fanxiu/wiki?tab=item` 时，地址栏和请求中不能残留非空 `type_key/sub_type_key`，页面应回到全量道具列表。这是防止“分享链接/刷新/验证入口被本地缓存污染”的硬约束。
- 道具列表图标 URL 必须和 `/api/fanxiu/resources/items/cards` 返回的 `icon/small_icon` 对齐，不能用 OCR、抓包字段或前端猜测替代。
- 道具列表图标验收必须优先按 `item_id` 对齐 API 期望图标；标题和序号只能作为降级兜底。同名道具、滚动采样和分类切换都不能靠标题猜图标。
- 道具图标浏览器验收要先等待当前可见行图片加载稳定，再采样 DOM。可见行缺 `data-item-id`、图片等待超时、真实 `src` 和期望 `icon` 不一致都必须失败。
- 道具图标加载成功后必须隐藏单字 fallback 层。透明 PNG 不能露出背后的“云/玄”等占位字，否则用户会误判为图标缺失或默认图。
- 邮件正文不能静默为空。真实抓包没有正文时，也要展示可解释摘要，例如“附件：...”或“抓包未携带正文或附件；邮件类型：...”。
- 邮件附件图片加载失败时，前端必须隐藏坏图并做有限退避重试；不能把浏览器破图状态留在用户可见界面里。
- 浏览器验收必须打开真实 `/fanxiu/wiki` 页面并检查 DOM、图片自然尺寸、正文弹窗和附件跳转，不能用单纯 API 成功代替页面成功。
- 除邮件和道具专项验收外，还必须用真实浏览器打开功法、活动、灵界、数字门、斗破等主图鉴 tab，确认页面有观测结果、无请求失败、无可见坏图。
- 核心图鉴 tab 浏览器验收不能只看 `observation_count > 0`。功法、活动、灵界、数字门、斗破 5 个 tab 必须都有资源列表行；其中功法、灵界、数字门、斗破 4 个 icon 资源 tab 必须至少有一个可见真实图片。
- 邮件浏览器验收必须覆盖分页后的全部可见邮件页，不只看第一页。当前 `verify_fanxiu_wiki_browser.py --tab mail` 默认最多检查 20 页，足够覆盖当前 116 条邮件记录。
- 总质量闸门会做跨层一致性检查：邮件浏览器验收后会立即再跑一次 `verify_fanxiu_mail_records.py` 作为 `mail_records_final`。浏览器渲染邮件数/附件图标数/附件链接数/正文按钮数必须落在浏览器前快照和浏览器后快照之间。抓包服务常开时邮件可能在长质量门运行期间新增，不能把“后置快照新增”误判成浏览器漏渲染；但浏览器数量低于前置快照或高于后置快照仍必须失败。
- 总质量闸门不能只相信子脚本退出码。每个子脚本最后输出的 JSON 摘要也要被读取并做硬断言；例如邮件正文覆盖、附件字段完整、图标接口失败数、浏览器坏图数、道具 `item_id` 对齐、fallback 可见数都必须满足验收条件。
- 总质量闸门默认还会运行前端 `typecheck` 和 `build`。图鉴页面是前端页面，不能只用 API/浏览器局部脚本通过来替代完整前端构建通过。
- 总质量闸门允许对明确的服务层瞬断做一次整步重试，例如后端重载时的连接拒绝或读超时；但真实图标缺失、稳定接口 4xx/5xx、页面渲染失败和跨层计数不一致不能被重试吞掉。全量 icon 检查中单个 icon 首轮失败时会冷却后复查一次，复查仍失败才计入 `icon_endpoint_failures`。

当前推荐命令：

```bash
uv run python scripts/verify_fanxiu_wiki_quality.py
```

需要单点定位时再拆开跑：

```bash
uv run python scripts/verify_fanxiu_wiki_quality.py --skip-browser --skip-frontend
uv run python scripts/verify_fanxiu_reverse_boundary.py
uv run python scripts/verify_fanxiu_reverse_manifest.py
npm run typecheck --prefix frontend
npm run build --prefix frontend
uv run python scripts/verify_fanxiu_mail_records.py --api-timeout 15
uv run python scripts/verify_fanxiu_card_catalogs.py --api-timeout 15
uv run python scripts/verify_fanxiu_resource_links.py --api-timeout 15 --wait-ms 12000 --samples-per-type 5 --required-resource item:3080008 --mail-reward-resource-samples 8 --icon-review-resource-samples 8
uv run python scripts/verify_fanxiu_item_icons.py
uv run python scripts/build_fanxiu_item_icon_quality_report.py --threshold 50
uv run python scripts/verify_fanxiu_wiki_icons.py --full-items --max-icons 0 --api-timeout 60
uv run python scripts/verify_fanxiu_wiki_endpoints.py --timeout 60
uv run python scripts/verify_fanxiu_wiki_browser.py --tab mail --local-auth-user admin --scroll-steps 1 --wait-ms 10000 --screenshot
uv run python scripts/verify_fanxiu_wiki_browser.py --tab item --item-type-label 云梦论剑 --item-type-label 时装 --item-type-label 礼包宝匣 --item-type-label 材料 --item-type-label 功法 --scroll-steps 2 --wait-ms 12000 --screenshot
uv run python scripts/verify_fanxiu_wiki_browser.py --tab gongfa --tab activity --tab lingjie --tab digitdoor --tab doupotd --scroll-steps 1 --wait-ms 10000
```

验收通过时应重点看这些计数：

- `verify_fanxiu_reverse_boundary.py`：
  - `ok == true`
  - `resource_root == D:\home\chenkunze\data\m2606凡修逆向\frxx_game_files`
  - `export_root == D:\home\chenkunze\data\m2606凡修逆向\frxx_analysis_exports`
  - `missing_resource_paths == []`
  - `missing_export_paths == []`
  - `failures == []`
  - 空的 `frxx_game_f` 这类误名目录只算 warning；一旦非空或被选为根目录，必须作为失败处理。
- `verify_fanxiu_reverse_manifest.py`：
  - `ok == true`
  - `raw_input_count > 0`
  - `hashed_file_count > 0`
  - `missing_count == 0`
  - `manifest_digest` 是 64 位 SHA256
  - `output_dir` 指向 `frxx_analysis_exports/parsed_configs/reverse_manifest_audit`
  - 当前真实样本为 `entry_count=34`、`raw_input_count=13`、`hashed_file_count=26`、`directory_summary_count=8`。
- `verify_fanxiu_mail_records.py`：
  - `records > 0`；这里的 `records` 是有正文或附件的可展示邮件
  - `raw_records >= records`
  - `empty_action_records >= 0`；这些是无正文、无已解析附件的抓包动作占位，只保留作取证，不进入默认 wiki 邮件列表质量计数。即使只带 `mail_rewards_unresolved` 或 `has_attachment_hint` 这类未解析附件提示，也不能算默认可展示邮件。
  - `with_content == records`
  - `reward_items_with_icon == reward_items`
  - `reward_items_with_name == reward_items`
  - `reward_items_with_item_id == reward_items`
  - `missing_content_records == []`
  - `missing_reward_fields == []`
  - `icon_endpoint_failures == []`
  - `/api/fanxiu/mail-records` 默认只返回可展示邮件；需要检查空动作占位或未解析附件提示时显式传 `include_empty_actions=true`。不要再让 `未知邮件动作...` 且无正文/已解析附件的占位污染邮件 wiki 页面。
  - 当前真实样本为 `records=170`、`raw_records=221`、`empty_action_records=51`、`reward_items=670`、`with_content=170`。
- `verify_fanxiu_wiki_icons.py`：
  - `icon_endpoint_failures == 0`
  - `api_endpoint_errors == 0`
  - `--full-items` 下，道具 icon 来自完整本地 `item_catalog`；功法、灵界、数字门角色、斗破角色来自分页后的完整 API 目录，不能只取首页。
- `verify_fanxiu_item_icons.py`：
  - `card_count > 0`
  - `unique_icon_count > 0`
  - `ok == unique_icon_count`
  - `fail == 0`
  - 当前真实样本为 `card_count=10030`、`unique_icon_count=3295`、`alias_count=3`。
  - 该检查验证的是“配置 icon 名称能从本地逆向资源导出”；`verify_fanxiu_wiki_icons.py` 验证的是“后端图标接口能返回图片”；浏览器脚本验证的是“页面 DOM 真实加载了正确图片”。三层都要保留。
- `build_fanxiu_item_icon_quality_report.py`：
  - `item_count > 0`
  - `group_count > 0`
  - `primary_group_count > 0`
  - `small_group_count > 0`
  - 当前真实样本为 `group_count=21`、`primary_group_count=9`、`small_group_count=12`、`high_priority_count=10`、`medium_priority_count=6`、`low_priority_count=5`。
  - 当前真实候选资产样本为 `candidate_group_count=18`、`candidate_icon_total=54`、`no_candidate_group_count=3`、`unresolved_no_candidate_group_count=3`、`no_candidate_review_status=pending_manual_decision`。候选来自 `frxx_analysis_exports/icons/*.png` 的同编号/同族名静态资产，例如 `icon_item_zw_0180` 可提示 `icon3_item_0180`、`icon8_item_0180` 这类现有变体；候选只用于人工复核，不自动替换配置。
  - 当前真实近邻上下文样本为 `nearby_context_group_count=16`、`nearby_context_icon_total=97`。`nearby_icons` 是同系列数字近邻，只用于人工视觉比对，不能当作自动替换候选。
  - `summary_latest.json` 和 Markdown 必须列出 `no_candidate_groups` 明细，不能只保留数量。每条无候选明细必须带 `review_status=pending_manual_decision`、`suggested_manual_action` 和 `remaining_risk`，表示“已治理到可复核，但还没有人工替换/豁免结论”。当前 3 个无现有资产候选组是 `icon2_item_9034`（高优先，样本 `1300605 / 灵材宝匣`，建议 `continue_static_asset_search`，近邻 `icon2_item_9033/9035/...`）、`icon5_item_zw_9716`（高优先，样本 `5012047 / 芙莲神心诀`，建议 `continue_static_asset_search`，近邻 `icon5_item_9715/9714/...`）、`icon5_item_9503`（中优先，样本 `20401001 / 攻击玉·陈巧倩`，建议 `review_exemption_candidate`，近邻 `icon5_item_9502/9504/...`）。
  - Markdown 的 `No Candidate Groups` 表必须带目标图标和近邻图标预览，便于人工判定“通用图/误用/需继续逆向”。
  - 同一报告目录下必须生成 `item_icon_no_candidate_contact_sheet_latest.png`，作为无候选组的一屏人工复核图；它只辅助判定，不改变配置图标。质量门禁必须验证该路径存在、文件非空、Pillow 可读取且尺寸有效。
  - 报告位置为 `frxx_analysis_exports/parsed_configs/item_catalog/icon_quality_review/item_icon_quality_review_latest.tsv|md`。该报告只做治理候选分组，不自动替换图标；高优先级和 `pending_manual_decision` 表示需要继续逆向或人工判定，不能当作资源接口失败，也不能当作已经完成替换。
  - `/api/fanxiu/resources/items/icon-quality-review` 必须能读取同一份报告，并在请求阈值变化时重建，避免前端治理面板读到旧阈值缓存。
- `verify_fanxiu_wiki_endpoints.py`：
  - `endpoint_count > 0`
  - `failure_count == 0`
  - 核心 endpoint 的样本行必须有关键字段；缺字段即使接口返回 `200` 也算失败。
- `verify_fanxiu_card_catalogs.py`：
  - `catalog_count > 0`
  - `total_cards > 0`
  - `sample_count > 0`；当前默认 6 类各 8 条，共 `48`
  - `details_ok > 0`；当前默认除活动外 5 类各 8 条，共 `40`
  - `detail_failure_count == 0`
  - `field_failure_count == 0`
  - `icon_failure_count == 0`
  - 当前默认覆盖功法、道具、活动、灵界、数字门角色、斗破角色六类主卡片目录，样本来自首段/中段/尾段，不只看第一页前几条。
- `verify_fanxiu_resource_links.py`：
  - `resource_count > 0`
  - `required_resource_count >= 1`
  - `required_resource_failure_count == 0`
  - `mail_reward_resource_count >= 8`
  - `mail_reward_resource_failure_count == 0`
  - `icon_review_resource_count >= 8`
  - `icon_review_resource_failure_count == 0`
  - `loaded_icon_count > 0`
  - `loaded_icon_count == required_icon_count`
  - `failure_count == 0`
  - `title_mismatch_count == 0`
  - `icon_mismatch_count == 0`
  - 当前默认每类抽 5 条，覆盖 `/fanxiu-resource/gongfa/...`、`/fanxiu-resource/item/...`、`/fanxiu-resource/activity/...`、`/fanxiu-resource/digitdoor/...`、`/fanxiu-resource/doupotd/...`、`/fanxiu-resource/lingjie/...`。
  - 必须固定覆盖 `/fanxiu-resource/item/3080008`。该页应渲染 `潜修心得·四刻`、`ID 3080008 道具`、`简介|字段|效果`，并加载 `icon_item_0285`。
  - 必须抽样覆盖抓包邮件附件里的真实 item id，避免“邮件列表能显示附件，但附件跳独立道具页不可用”的断层。
  - 必须抽样覆盖道具图标治理报告里的代表样本 item id，避免“治理按钮能筛选，但代表样本独立页不可用”的断层。抽样应优先跨治理组各取一个代表样本，而不是只取最大组的前几条。当前真实样本包括 `37092001 / 炎帝法印 / small_icon:mainui_icon_zw_1020`、`15031003 / 极品镇邪宝箱 / icon:icon_item_zw_0180`、`8017506 / 圣·白骨精 / small_icon:mainui_icon_zw_1022`。
- `verify_fanxiu_wiki_browser.py --tab item ...`：
  - `item_route_filter_count > 0`
  - `item_route_filter_failure_count == 0`
  - `item_route_clear_count > 0`
  - `item_route_clear_failure_count == 0`
  - `item_icon_route_check_count >= 2`
  - `item_icon_route_check_failure_count == 0`
  - `item_icon_review_count >= 1`
  - `item_icon_review_failure_count == 0`
  - `item_row_icon_missing_count == 0`
  - `item_row_icon_mismatch_count == 0`
  - `item_row_icon_wait_timeout_count == 0`
  - `item_row_missing_id_count == 0`
  - `item_row_fallback_visible_count == 0`
  - `broken_visible_image_count == 0`
  - `request_failure_count == 0`
  - `browser_item_row_icons_latest.tsv` 的 `item_id` 应等于 `expected_item_id`，`item_icon` 应等于 `expected_icon`。
  - `browser_item_row_icons_latest.tsv` 的 `image_wait_ok == true` 且 `image_wait_pending_count == 0`。
  - `browser_item_row_icons_latest.tsv` 的可见行 `fallback_visible == false`。
  - `browser_item_row_icons_latest.tsv` 的 `sample_step` 应覆盖滚动采样点，而不只是初始首屏。
  - `browser_item_route_filters_latest.tsv` 应显示带筛选 URL 的 `href/active_labels/request_rows` 和后端解析出的分类 key 一致。
  - `browser_item_route_clear_latest.tsv` 应显示无筛选 URL 的 `active_labels` 为空，且请求中没有非空 `type_key/sub_type_key`。
  - `browser_item_icon_route_checks_latest.tsv` 应显示主图标高复用路由 `/fanxiu/wiki?tab=item&icon_quality=high_reuse&icon_name=icon_item_zw_0153` 和小图标高复用路由 `/fanxiu/wiki?tab=item&small_icon_quality=high_reuse&small_icon_name=mainui_icon_zw_1020` 都能复现 URL 参数、激活筛选、预期总数、图标组按钮和组缩略图加载。
  - `browser_item_icon_review_latest.tsv` 应显示道具页治理行已加载，有高优先候选摘要和可点击筛选按钮；若摘要包含 `无候选`，也必须包含 `待判定`，当前真实摘要为 `21 组 · 高优先 10 · 无候选 3 · 待判定 3 · 近邻 97`。候选按钮必须带 `样本：...` 代表道具和 `候选：...` 现有静态资产提示，不能只有笼统说明。点击治理候选后必须把第一个代表样本作为当前选中道具打开，`sample_href` 应带样本 `id`，`sample_detail_title` 应为真实道具名。当前重点候选包括 `mainui_icon_zw_1020`，其按钮标题和跳转链路应能看到 `37092001:炎帝法印`，并应有至少一个现有资产候选提示；候选缺失会计入 `item_icon_review_failure_count`。
  - 治理行还必须提供“复核图”链接，`browser_item_icon_review_latest.tsv.contact_href` 应指向 `/api/fanxiu/resources/wiki/media?...item_icon_no_candidate_contact_sheet_latest.png`，缺失、不是 media 接口、或有 `无候选` 但没有 `待判定` 会计入 `item_icon_review_failure_count`。
- `verify_fanxiu_wiki_browser.py --tab mail ...`：
  - `mail_row_count` 应覆盖当前邮件记录总量，当前样本为 `170`
  - `mail_reward_image_count` 应覆盖当前所有附件，当前样本为 `670`
  - 每页 `image_wait_ok == true` 且 `image_wait_pending_count == 0`
  - `mail_broken_visible_reward_image_count == 0`
  - `mail_missing_icon_slot_count == 0`
  - `mail_empty_alt_image_count == 0`
  - `mail_invalid_item_link_count == 0`
  - `mail_content_dialog_ok == true`
  - `mail_item_link_navigation_ok == true`
- `verify_fanxiu_wiki_browser.py --tab gongfa --tab activity --tab lingjie --tab digitdoor --tab doupotd ...`：
  - `observation_count > 0`
  - `core_tab_with_rows_count == core_tab_count`
  - `core_tab_with_visible_images_count == core_tab_image_required_count`
  - `request_failure_count == 0`
  - `broken_visible_image_count == 0`
  - `hard_image_failure_count == 0`
- `verify_fanxiu_wiki_quality.py`：
  - 当前默认 `step_count == 15`，包含 `reverse_boundary`、`reverse_manifest`、`item_icons`、`item_icon_quality_report` 和 `mail_records_final`。
  - 2026-06-08 最新真实通过样本：`records=170`、`raw_records=221`、`empty_action_records=51`、`reward_items=670`、`with_content=170`、`unique_icons=3403`、`item_icons.unique_icon_count=3295`、`item_icon_quality_report.group_count=21`、`item_icon_quality_report.high_priority_count=10`、`item_icon_quality_report.candidate_group_count=18`、`item_icon_quality_report.candidate_icon_total=54`、`item_icon_quality_report.unresolved_no_candidate_group_count=3`、`item_icon_quality_report.no_candidate_review_status=pending_manual_decision`、`item_icon_quality_report.nearby_context_group_count=16`、`item_icon_quality_report.nearby_context_icon_total=97`、`resource_links.resource_count=47`、`resource_links.required_resource_count=17`、`resource_links.mail_reward_resource_count=8`、`resource_links.icon_review_resource_count=8`、`mail_browser.mail_reward_image_count=670`、`item_browser.item_row_icon_count=765`、`item_browser.item_icon_route_check_count=2`、`core_browser.core_tab_with_rows_count=5`。
  - `consistency_failures == []`
  - `summary_failures == []`
  - 若 `steps[].retry_count > 0`，必须能从 `output_tail` 看出是连接拒绝/连接失败类瞬断，而不是资源或页面真实失败。
  - 即使某个子脚本 `returncode == 0`，只要摘要里出现 `with_content < records`、`icon_endpoint_failures > 0`、`item_icons.fail > 0`、`item_icons.ok != item_icons.unique_icon_count`、`item_icon_quality_report.group_count <= 0`、`item_icon_quality_report.candidate_group_count <= 0`、`item_icon_quality_report.candidate_icon_total <= 0`、`item_icon_quality_report.no_candidate_group_count > 0` 但缺少足量 `no_candidate_groups` 明细、缺少 `unresolved_no_candidate_group_count/no_candidate_review_status`、或明细缺少 `icon/review_priority/review_status/suggested_manual_action/remaining_risk/sample.id`、无候选 contact sheet 缺失/为空/不可读、`card_catalogs.icon_failure_count > 0`、`resource_links.failure_count > 0`、`resource_links.required_resource_failure_count > 0`、`resource_links.mail_reward_resource_failure_count > 0`、`resource_links.icon_review_resource_failure_count > 0`、`resource_links.title_mismatch_count > 0`、`resource_links.icon_mismatch_count > 0`、`item_route_filter_failure_count > 0`、`item_route_clear_failure_count > 0`、`item_icon_route_check_count < 2`、`item_icon_route_check_failure_count > 0`、`item_row_fallback_visible_count > 0`、`item_row_icon_mismatch_count > 0` 等关键异常，也必须作为 `quality_summary` 失败处理。
  - 如果浏览器渲染数低于邮件前置快照或高于邮件后置快照，必须作为 `cross_layer_consistency` 失败处理；如果后置快照因抓包新增而增长，浏览器数量落在前后快照区间内可通过。
  - 默认包含 `frontend_typecheck` 和 `frontend_build` 两个步骤，二者都必须 `returncode == 0`。

如果页面截图里短暂看到单字 fallback，但上述浏览器脚本显示真实图片已加载、自然宽高非零且图标 URL 匹配 API，则优先判断为加载过程或旧页面缓存；如果脚本也报缺失或错配，则按资源链路缺陷处理。
