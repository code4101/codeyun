# 凡修邮件抓包协议研究

本文记录凡修邮件界面的抓包协议、客户端 Lua 处理链、现有样本解析状态，以及后续自动化的安全边界。

## 当前界面与抓包对应

当前邮件界面截图显示底部文案为 `已锁定 5/10封邮件`。画面中至少可见 5 封邮件：

| 界面标题 | 抓包对应状态 | 说明 |
| --- | --- | --- |
| 活动道具自动转换 | 已出现 `SM_NewMail`，尚未完整解出 type | 当前解码器在该包的 `i18nParams` 附近失败；静态配置可反查候选 type 为 1057/1071/1072/1074/1075/1076/1077/1079/1080/1082。 |
| 参与击杀魔祖未取奖励 | `SM_NewMail` 完整解出 type 67 | `Envelope` 标题为 `参与击杀魔祖未取奖励`，当前未读且未领取。 |
| 仙缘夺魁位面榜奖励 | `SM_NewMail` 完整解出 type 1552 | `Envelope` 标题为 `仙缘夺魁位面榜奖励`，当前已读且已领取。 |
| 仙缘夺魁个人榜奖励 | 已出现 `SM_NewMail`，尚未完整解出 type | 静态配置中标题对应 type 1551；当前样本在 `i18nParams` 附近解码失败，不能只靠静态反查当成实包字段。 |
| 丹道问鼎奖励 | `SM_NewMail` 完整解出 type 1014 | `Envelope` 标题为 `丹道问鼎奖励`，当前未读且未领取。 |

结论：抓包里可以对应解析到当前界面的邮件来源，但目前不是 5 封都能完整字段级解析。已确认 3 封完整解析，2 封卡在通用 `readMessageList2List` 解码缺口上。

打开邮件界面本身不一定会立刻触发完整邮箱列表抓包。当前样本更像是客户端已经通过 `SM_MailBox` 初始化和后续 `SM_NewMail` 推送维护本地 `MailModel`，打开 UI 只是展示已有模型。

## 协议表

邮件协议位于 `common.mail`：

| id | 名称 | 方向 | 字段 | 客户端处理 |
| --- | --- | --- | --- | --- |
| 30402 | `SM_MailBox` | server -> client | `mailVos`, `locks` | `MailMgr.Model:SetMailBox(msg)` |
| 30404 | `SM_NewMail` | server -> client | `mailVo` | `MailMgr.Model:NewMail(msg)` |
| 30405 | `CM_ReadMail` | client -> server | `id` | 读取单封邮件 |
| 30406 | `SM_ReadMail` | server -> client | `id` | `MailMgr.Model:ReadMail(msg)` |
| 30407 | `CM_GetMailReward` | client -> server | `ids` | 领取邮件奖励 |
| 30408 | `SM_GetMailReward` | server -> client | `ids`, `bagFull`, `rewardResults` | `MailMgr.Model:OnGetRewardSucceed(msg)` |
| 30409 | `CM_DeleteMail` | client -> server | `ids` | 删除邮件 |
| 30410 | `SM_DeleteMail` | server -> client | `ids` | `MailMgr.Model:DeleteMail(msg)` |
| 30411 | `CM_IsMailExpired` | client -> server | `ids` | 查询是否过期 |
| 30412 | `SM_IsMailExpired` | server -> client | `ids` | 过期结果 |
| 30413 | `CM_LockMail` | client -> server | `id`, `lock` | 锁定/解锁邮件 |
| 30414 | `SM_LockMail` | server -> client | `id`, `lock` | `MailMgr.Model:LockMail(msg)` |
| 30451 | `MailVo` | VO | `id`, `type`, `title`, `content`, `createTime`, `expireTime`, `rewards`, `i18nParams`, `read`, `rewardGetted`, `senderName` | 邮件实体 |

## 客户端处理链

`MailNetLogic.lua` 注册所有邮件协议。关键入口：

- `SM_MailBoxFun(msg)`：调用 `MailMgr.Inst_get().Model:SetMailBox(msg)`。
- `SM_NewMailFun(msg)`：调用 `MailMgr.Inst_get().Model:NewMail(msg)`。
- `CM_ReadMailFun(id)`：发送 `CM_ReadMail.id=id`。
- `CM_GetMailRewardFun(ids)`：发送 `CM_GetMailReward.ids=ids`。
- `SM_GetMailRewardFun(msg)`：检查错误码；如果 `bagFull` 则提示背包满；成功后调用 `OnGetRewardSucceed`。

`MailData.lua` 维护本地邮件模型：

- `SetMailBox(msg)`：把 `msg.mailVos` 和 `msg.locks` 写入模型，再按 `createTime` 倒序排序。
- `NewMail(msg)`：把新邮件插入列表开头，再刷新红点。
- `OnGetRewardSucceed(msg)`：把奖励结果交给奖励管理器，并将命中的邮件标记为 `read=true`、`rewardGetted=true`。

`MailPrefab.lua` 和 `MailContentView.lua` 展示标题、内容、发件人：

- 列表标题优先使用 `MailVo.title`，为空时使用 `Envelope_Envelope[type].title`。
- 详情内容优先使用 `MailVo.content`，为空时通过 `Envelope_Envelope[type].contentId` 和 `PostMgr.GetPostMessage(...)` 生成。
- 发件人优先使用 `MailVo.senderName`，为空时使用 `Envelope_Envelope[type].sender`。
- 因此 packet 同步器必须能读取静态 `Envelope.Envelope` 表。若出现 `未知邮件类型123`，优先检查 `lscripts/generate/cfg/envelope_*.bytes` 是否已经导出并解析成 `parsed_configs/Envelope/rows.json`；不能用 OCR 截图文字或临时硬编码当作 packet 标题事实。

## 领取安全边界

邮件界面存在两个全局按钮：`一键删除` 和 `一键领取`。自动化中必须把 `一键领取` 当成不存在，不能点击；但 `一键删除` 不是同类风险按钮，它用于清理已经没有附件/已阅的邮件，是 `邮件_选择性领取` 的合法且应保留动作。

原因是 `WinMailBoxView.OnGetBtnClick` 会调用 `MailMgr:ClaimAllRewards()`，它会遍历所有未领取且未锁定的邮件，构造多个 id 后一次性发送 `CM_GetMailReward`。这会把“未锁定”错误等同于“可领”，不符合当前策略。

单封详情里的按钮才是安全路径：`MailContentView.OnClickBtn` 会构造只包含当前邮件 id 的 `idList`，如果当前邮件可领取，则发送 `CM_GetMailRewardFun(idList)`；否则发送单封删除。

`一键删除` 是可重复执行的安全清理动作：逐封领取进入 `#347` 奖励过场后，等待自动回到 `#121`，立即执行一次 `#121[一键删除]`；若进入 `#348`，点击 `#348[确认]` 并等待回到 `#121`。完整扫描结束时再执行一次相同闭环作为收尾。没有可删除邮件时按钮可能直接回到 `#121`，也按正常结果继续。单封详情页里的 `删除` 同样是合法清理动作。这些删除动作不能参与领取候选判定。

## 运行态处理策略

当前在线邮箱事实以严格只读的 `MailMgr` 内存快照为准：它提供完整邮件顺序、id、type、创建时间、附件、领取和锁定状态。静态 `Envelope`、`Item` 资源表负责补全标题与道具语义；packet 历史负责留痕和交叉验证。OCR 不决定附件内容、领取策略或邮件顺序，只负责把当前 `#121` 可见窗口对齐到 `MailMgr` 有序清单。

标准 `邮件_选择性领取` 策略只看邮件图鉴数据库里的状态标记：只有标为 `可领` 的邮件才允许领取；`锁定`、`留存`、附件未知、非锁定状态、附件内容或可见列表断层都不能参与领取判定。删除是另一类清理动作：通过单封详情页 `删除` 或扫描结束后的 `一键删除` 清掉已阅/无附件邮件。不要把它实现成“除锁定外全部领取”。

图鉴展示层不能把“抓包里没有正文模板”渲染成空白。当前固定规则：

- `MailVo.content` 或静态 `Envelope.contentId` 能解析出正文时，展示解析后的正文。
- 抓包没有正文但有附件时，展示附件摘要，例如 `附件：灵石 x128`、`自动领取：试炼手册 x360`。
- 抓包既没有正文也没有附件时，展示透明说明，例如 `抓包未携带正文或附件；邮件类型：秘境封魔杀报名提醒`。
- 不为了填空伪造游戏正文，不把 OCR 截图文字当成 packet 正文。
- 前端邮件图鉴的“正文”列可以压缩为“查看”按钮，但弹窗打开后必须能看到上述正文或摘要；验证脚本会把空正文视为失败。

当前自动领取/删除判断按以下优先级顺序执行：

1. 附件名称或类别带 `法则`：最高优先级保护，永远不能自动领取。后续应先锁定，避免手误或一键领取误触；锁定操作流程尚未固化。锁定上限为 10 封，若候选超过上限，应优先锁定更高价值法则，例如跨服法则优先于服内法则，具体等级规则后续再核对。
2. 附件带 `潜修心得·四刻`：可以自动领取。该规则优先于保护资源判断，但仍低于法则保护。
3. 附件含下列祈愿相关保护资源：暂缓，不自动领取。
   - 炼丹：`炼丹灵草匣`、`神品灵草匣`、`炼丹灵草宝匣`
   - 淬体：`淬体精魄`
   - 灵兽：`珍品饲灵丸`
   - 洗灵：`洗灵奇石`
   - 仙花：`瑶池玉莲`、`造化青莲`
4. 其余有附件邮件：packet 已明确解析附件名称，且不命中以上规则时，可单封自动领取。

无附件邮件默认直接删除。若界面按钮文案叫“领取”，也按“无附件可处理”理解。

安全边界：如果 packet 没解析出明确道具名，暂缓处理，不能靠 OCR 或截图猜测。

运行态校验边界：

- 行为树先从 `MailMgr` 完整有序快照生成单封领取目标；`#121` 的 OCR 标题和时间只用于确认当前窗口对应哪个连续子序列。
- OCR 已观察到目标标题时，点击本帧标题框中心；只有标题被遮挡、但前后锚点能唯一确定目标槽位时，才使用固定槽位中心。OCR 只修正落点，不得反向改变 `MailMgr` 选出的目标。
- 如果可见邮件没有匹配到精确 packet 事实，自动化必须把本轮标记为 `packet_gap`，记录可见标题、时间和缺口原因，然后停止证明“已清干净”。
- 不允许用历史同名、同类型、相邻时间或截图附件图标去推断附件事实；这类缺口属于抓包/解析/历史补扫问题，不属于游戏 UI 硬兜底问题。
- DB 中 `claimed/deleted/missing_from_list` 只代表某个具体 `mail_key` 已观察到的结果，不能反向证明游戏里已经没有同标题同分钟的其它邮件；可见行仍要重新匹配。存在重复邮件时，终态记录只作历史证据，可见行必须绑定下一条尚未终结的精确 packet 记录；没有活动记录时停止动作，绝不能把同一个终态 `mail_key` 改回 `claim_requested` 后重复领取。

## 清单对齐与滚动约束

`邮件_选择性领取` 的三层真值不能合并或删减：

1. `MailMgr` 是业务真值，决定领取哪一封及处理顺序。
2. `visual_alignment.py` 使用 OCR 标题、时间和固定行栅格，把可见窗口配准到 `MailMgr` 的一个连续子序列。
3. 单封详情页必须出现唯一“领取”动作，才允许点击并确认本次领取；列表配准不能代替详情动作复核。

滚动后的读取遵循固定顺序：`拖拽 → 至少等待 1.5 秒 → 截取新帧 → OCR/配准 → 点击`。游戏横幅、飘字和倒计时可能持续变化，因此整帧稳定度只能作为有次数上限的辅助证据，不能成为无限等待的硬门槛。业务代码不得在 `drag_shape_content()` 后立即截图；应统一调用 `scroll_shape_content()`，邮件局部小幅复位也不例外。

`一键删除` 只删除已领或无附件邮件，不会把当前滚动位置复位到顶部。执行清理后若要重建 offset=0，必须退出到 `#34` 再重新进入 `#121`；禁止留在原地却把当前窗口标成顶部。重新进入后首行若被横幅遮挡，可由确定的顶部锚点和 `MailMgr` 顺序推断槽位，但详情页仍需复核唯一“领取”动作。

点击邮件后若仍停在 `#121`，说明落点未命中，不代表策略应跳过该邮件。应等待画面、重新读取当前窗口并重试同一目标；达到有界重试次数后安全失败，不能继续猜点下一封。

2026-08-10 真实验收：初始 `MailMgr` 有 55 封，其中 16 封被策略判定为必领；工程重试结束后剩余 39 封、必领数为 0。具名确认包括“分红发放”“宗门镇邪活动奖励”“宗门灵泉活动收益”。本次故障来自清理后原地继续却误标 offset=0，以及局部拖拽绕过统一滚动等待；不是动态清单或领取策略丢失。

未决事项：

- 法则邮件的锁定入口、确认流程和失败恢复。
- 法则价值排序：跨服、服内、活动来源、等级或稀有度的具体优先级。
- 保护资源清单的继续扩展。当前只记录已在邮件样本中确认的名称，不假设完整图鉴分类已经准确。
- `祈愿` 活动如何消耗或影响这些资源，以及对应邮件何时可以领取。

## 当前样本

当前数据库中邮件相关已解码记录：

| 时间 | 协议 | 数量/状态 | 样本 |
| --- | --- | --- | --- |
| 2026-06-04 17:25:44 | `SM_MailBox` | 1 条，未完整解析 | `payload_len=4770`，解析到 304 字节后失败。 |
| 2026-06-04 17:25:44 | `SM_NewMail` | 1 条，完整解析 | type 9999，标题 `【停服维护补偿】`。 |
| 2026-06-05 00:00:42 | `SM_NewMail` | 5 条 | 3 条完整解析，2 条在 `i18nParams` 附近失败。 |
| 2026-06-05 01:01:54、01:07:19 | `CM_ReadMail` / `SM_ReadMail` | 各 2 条 | 单封阅读行为。 |

截至当前样本，数据库中还没有观察到 `CM_GetMailReward` / `SM_GetMailReward`，所以尚不能用真实抓包确认单封领取结果包的字段表现。

## 解码器缺口

当前通用解码器对 `readMessageList2List` 的基本模型是对的：读取长度、共享类型标记、共享 proId 或逐项 proId，再按基础类型或消息 Bean 解析。

但邮件样本暴露了两个缺口：

- `SM_MailBox.locks` 在 Lua 读法上写成 `readMessageList2List(self.locks)`，但写法是 `writeLongList(self.locks)`。仅按读法当成消息 Bean 列表会误读，应该结合 writing 元信息或包字段特例把它按 LongList 解析。
- 部分 `MailVo.i18nParams` 的 `I18nParam2Num` 解析结果里，`key` 出现了 `\0...NUM` 这样的错位字符串，且另外两条 `SM_NewMail` 在同一区域失败。2026-06-05 用真实 `SM_MailBox` 样本校准后确认：当前运行包里的 `I18nParam2Num.value` 是 8 字节 `Double`，不是旧 Lua 导出的 `Int`。按 `Double` 读取后，样本中的 `SM_MailBox` 可以连续解出 78 封邮件和 5 个锁定 id。

后续修复顺序：

1. 为通用解码器补 `writeLongList` 字段提示，让 `SM_MailBox.locks`、`CM_GetMailReward.ids`、`SM_GetMailReward.ids` 等字段按 LongList 解析。
2. 针对 `I18nParam2Num.value` 按真实运行包补 `Double` 解码补丁，不能再按旧导出的 `Int` 或临时猜测的 `Long` 读取。
3. 重新解码历史 pcap，并回填 `FanxiuPacketDecodedRecord`，让当前 5 封邮件都能落到完整 type/title/read/rewardGetted 字段。

## 自动化原则

- 不使用 Android 返回键或 Esc；在凡修中它们是退出游戏用途。
- 不点击 `一键领取`。
- 保留 `一键删除`：它是清理已阅/无附件邮件的合法收尾动作，和 `一键领取` 的批量领取风险不同。
- 捕获到 `#347` 奖励过场时先等待它自动关闭并回到 `#121`，随后执行 `#121[一键删除]`；若出现 `#348`，点击 `确认` 后必须重新等待 `#121`，再继续邮件扫描。
- 保留单封详情页 `删除`：它用于删除已经确认无附件/不可领取的单封邮件，和 `一键删除` 同属清理动作。
- `一键删除` 合法不代表未锁定邮件都可领取；领取候选只能来自邮件图鉴数据库状态 `可领`，不能由可见列表断层、非锁定状态、附件内容或留存状态自动推导。
- 邮件打开后优先通过场景 #121 和具体 shape/OCR 做 UI 验证。
- 抓包验证应关注 `SM_MailBox`、`SM_NewMail`、`CM_ReadMail`、`CM_GetMailReward`、`SM_GetMailReward`，不要只看点击打开邮件界面的瞬间是否出现新包。
- 邮件图鉴验收应同时跑数据层和页面层：
  - `uv run python scripts/verify_fanxiu_mail_records.py --api-timeout 15`
  - `uv run python scripts/verify_fanxiu_wiki_browser.py --tab mail --local-auth-user admin --scroll-steps 1 --wait-ms 10000 --screenshot`
- 数据层必须满足 `with_content == records`，页面层必须满足正文弹窗可打开且附件图标、附件链接都有效。
