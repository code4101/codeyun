# 拜谒与行为树遗留验证

本页保留旧自动化交接链接的最小兼容入口，不再记录逐轮心跳、截图路径、完成项和运行流水。

仍需在真实可操作窗口验证：

- `#265` 返回 Shape 的正式匹配与真实落点；
- 滚动窗口“新帧 → 局部 OCR → 候选复核”的完整证据；
- `wait_click` 局部转场成功链；
- 拜谒旋转选择态中“魔道”词框及推导点击点。

执行时遵守 [fanxiu skill](../../../../../skills/fanxiu/SKILL.md)：

- 已有资产默认只读，修改必须取得点名授权；
- 不恢复旧作业中间状态；
- 到真实场景后使用最小 Cell 验证一个假设；
- 结果进入代码、测试和正式资产，完成后删除本兼容入口。

历史细节从 Git 历史或本轮减肥备份追溯。
