# 凡修拜谒与行为树基础设施任务清单

更新时间：2026-06-23 11:12

本文记录当前凡修自动化的短期执行清单。它不是新的长期架构约定；长期约定仍以 `凡修行为树业务能力约定.md`、`凡修行为树运行框架约定.md`、`凡修data-annotation自动化Runtime与Scheduler.md` 为准。

## 总原则

- 先清理真实运行卡点，再改基础设施，最后回到拜谒功能闭环。
- 所有会操作 MuMu/ADB、Runtime、Scheduler、行为树的事项串行执行。
- 开始任何维护项前先确认 Runtime、手动作业队列、守护和行为树 owner 为空闲；不抢占正在运行的任务。
- 后续接力顺序固定为：先执行已到期业务作业；确认行为树空闲后再执行前面留下的基础设施/文档维护任务；最后再回到拜谒 OCR 精细定位与 `魔道` 实测。
- 只用公开 Runtime/行为树入口和真实画面验证，不用私有函数或静态截图冒充成功。
- 遇到缺标注、场景 unknown、点击目标不可靠时停止记录证据，不硬点。
- `2026-06-23 03:30` 以后暂停拜谒实机点击调整；不要继续为了验证拜谒而硬点 #265 或绕过 #265「返回」匹配。

## 阶段 0：恢复 0 点到期作业

完成事实：

- `日常_助手` 已完成，`last_run_at=2026-06-23 00:58:05`。
- `日常_游历` 的奖励找回弹窗已通过 OCR 守卫后点击既有 `#69「退出」` 标注关闭，未再停留在 `unknown`。
- `邮件_清理` 已完成，`last_run_at=2026-06-23 01:24:00`，下一次 `2026-06-24 00:05:00`。
- `日常_游历` 已重新从 `#34 世界` 正常闭环完成，`last_run_at=2026-06-23 01:25:33`，下一次 `2026-06-23 05:00:00`。
- `doctor --json` 于 `2026-06-23 01:25:42` 确认 `due_task_count=0`，Runtime 空闲且当前场景 `#34 世界`。
- 默认作业迁移刷新后，`legacy-daily-assistant` 又以 `next_time=2026-06-23 00:00:00` 暴露为到期项；已串行执行 `日常_助手`，`last_run_at=2026-06-23 02:01:38`，下一次 `2026-06-23 06:00:00`。
- `doctor --json` 于 `2026-06-23 02:01:48` 再次确认 `due_task_count=0`，Runtime 空闲且当前场景 `#69 日常`。
- `doctor --json` 于 `2026-06-23 02:16:58` 再次确认 `due_task_count=0`；后端已重载，行为树服务待恢复，当前场景暂为 `null`。
- 后端重载后 0 点任务再次暴露为到期；`watch-doctor --auto-run-due` 于 `2026-06-23 02:20-02:23` 串行完成 `日常_助手`，`last_run_at=2026-06-23 02:23:21`，下一次 `2026-06-23 06:00:00`。
- 同轮 `日常_游历` 先只清掉奖励找回页并返回 `skipped`：`奖励找回页退出只证明已完成清理，不是游历完成证据`；未把这次当完成。
- `mail-check --wait` 于 `2026-06-23 02:24-02:27` 完成 `邮件_清理`，见到 75 封、领取 2 封、滚动 17 次，`last_run_at=2026-06-23 02:27:14`，下一次 `2026-06-24 00:05:00`。
- 第二次 `watch-doctor --auto-run-due --auto-run-due-min-interval-seconds 0` 于 `2026-06-23 02:27-02:29` 从 `#34 世界` 跑通 `日常_游历`，最终 `游历已完成并回到世界`，`last_run_at=2026-06-23 02:28:56`，下一次 `2026-06-23 05:00:00`。
- `doctor --json` 于 `2026-06-23 02:29:15` 确认 `due_task_count=0`，Runtime 空闲，当前场景 `#34 世界`。
- `watch-doctor --max-iterations 1 --auto-run-due` 于 `2026-06-23 02:57:53` 再次确认 `due=0`，AI 保底入口未提交新作业。
- `doctor --json` 于 `2026-06-23 03:12:58` 再次确认 `due_tasks=[]`，Runtime/owner/manual_jobs/隔离锁均为空闲，当前场景 `#265`；工程作业组关闭但 AI 保底无需提交新作业。
- `doctor --json` 于 `2026-06-23 03:30:18` 再次确认 `due_tasks=[]`，Runtime/owner/manual_jobs/隔离锁均为空闲，当前没有到期作业；后端已重载，行为树服务待恢复，下一批日常多在 `2026-06-23 05:00:00` 或之后。
- `watch-doctor --max-iterations 1 --auto-run-due` 于 `2026-06-23 03:33:21` 通过 AI 保底入口确认 `severity=ok due=0 stale=0 blocked_due=0`，没有提交新作业；证据写入 `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu-watch\doctor_watch_20260623_033321.latest.json`。
- `doctor --json` 于 `2026-06-23 03:34:26` 再次确认 `due_tasks=[]`，Runtime/owner/manual_jobs/隔离锁均为空闲；最近下一批日常仍为 `2026-06-23 05:00:00`。
- `watch-doctor --max-iterations 1 --auto-run-due` 于 `2026-06-23 03:34:50` 通过 AI 保底入口确认 `severity=ok due=0 stale=0 blocked_due=0`，没有提交新作业；证据写入 `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu-watch\doctor_watch_20260623_033450.latest.json`。
- `doctor --json` 于 `2026-06-23 03:35:53` 再次确认 `due_tasks=[]`，Runtime/owner/manual_jobs/隔离锁均为空闲；最近下一批日常仍为 `2026-06-23 05:00:00`。
- `watch-doctor --max-iterations 1 --auto-run-due` 于 `2026-06-23 03:36:15` 通过 AI 保底入口确认 `severity=ok due=0 stale=0 blocked_due=0`，没有提交新作业；证据写入 `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu-watch\doctor_watch_20260623_033615.latest.json`。
- `2026-06-23 11:03` 心跳保底复查发现新一批到期任务已出现：`doctor --json` 显示 `due_tasks=17`，并存在陈旧 owner（非凡修常驻服务进程）。已先停止并清理本轮遗留进程/owner，再通过公开 `watch-doctor --max-iterations 1 --auto-run-due` 串行接管到期作业。
- `2026-06-23 11:06-11:09` AI 保底串行执行后，`日常_灵塔` 成功完成并写回下一次 `2026-06-24 05:00:00`；随后 `日常_灵祖` 被本轮清理 stop 请求中断，`日常_双修` 报错 `当前不在可识别的世界、日常页或双修秘术页，无法开始`。证据写入 `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu-watch\doctor_watch_20260623_110613.latest.json`。
- `2026-06-23 11:10` 复查期间保底入口继续接管 `日常_报名`，真实点击 #86「确认」后回到 #34，但 Runtime 中断并报告：`场景跳转实际到达 #34，可继续规划到 #69，但这是「空白」的未声明落点，不在 sceneJumpTarget 中`。这属于点击/转场基础设施应暴露的缺标注或错标注问题；当前不继续抢跑剩余到期任务，也不进入后续维护。
- `2026-06-23 11:11` 通过公开 `stop` 与短时 `service --duration-seconds 8` 收尾，Runtime 回到 `idle`，当前场景 `#34 world 100%`，但剩余到期作业仍未清空，且 `日常_报名` 转场标注问题需要人工确认。

证据：

- `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu_unknown\20260623\1782147525336_wait_click.png`
- `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu_unknown\20260623\1782147525336_wait_click.json`
- `C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu-watch\manual_20260623_0053.latest.json`

待办：

- [x] 给奖励找回弹窗补可靠识别或复用已有标注，确保它不再被当作设置页。
- [x] 设计低风险关闭路径：优先使用已标注关闭/空白/确定语义；没有可靠标注时先补标，不用 Android Back。
- [x] 恢复到 `#69 日常` 或可规划场景。
- [x] 单独执行 `邮件_清理`，并等待 queued job 结束。
- [x] 复查 Scheduler plan，确认 0 点和 00:05 到期项清空或有明确 retry/blocker。

验收：

- `uv run python scripts/fanxiu_bt.py doctor --json` 中 `due_task_count=0`，或剩余 due task 有明确 `blocked_due_ids` 和证据。
- Runtime 不停留在奖励找回弹窗或 `unknown` 卡点。

## 阶段 1：滚动窗口安全查找基础设施

问题背景：

- 日常窗口、拜谒入口、邮件等都依赖滚动查找。
- 弹窗遮挡区域会污染 OCR，滚动后应尽量只分析非遮挡识别区。
- 默认滚动逻辑应统一：50% 手势比例，`duration` 1-1.5 秒；业务代码不应临时自定义比例和时长。

待办：

- [x] 梳理现有 `scroll_shape`、`ocr_in_shape`、`find_text_in_scroll_shape` 或同类接口，当前统一入口是 `FanxiuRuntime.scroll_shape_content()`，日常入口查找、拜谒 #264、报名、邮件都应优先走它。
- [x] 补齐统一默认滚动参数：50% 比例，`duration` 1-1.5 秒；已收敛为 Runtime 常量，当前默认 `ratio=0.5`、`duration=1.5s`、`settle_seconds=1.0s`。
- [x] 让调用方只声明滚动容器、方向、目标和识别区，不自行计算手势；已去掉 `邮件_清理` 中局部覆盖的 `ratio=0.5/duration=0.9/settle_seconds=0.35`，改回 Runtime 默认滚动入口。
- [x] 支持滚动后只分析指定识别区，减少弹窗遮挡区 OCR 误判；`scroll_shape_content(..., recognition_shape="识别区")` 手势仍作用在滚动容器，签名/变更判断只看识别区。
- [x] 将候选命中但靠近边缘的“小幅复位”作为候选后处理，而不是替代默认滚动；新增 `nudge_shape_content_for_box(...)`，复位后仍需重新截图/OCR 复核。

验收：

- 单元回归已验证 `scroll_shape_content()` 默认产生半页滚动、1500ms 拖拽、滚动后等待 1s。
- 单元回归已验证 `scroll_shape_content(..., recognition_shape=...)` 使用滚动容器执行 50% 默认手势，但只对识别区计算滚动前后签名。
- 单元回归已验证 `nudge_shape_content_for_box(...)` 只在候选贴近边缘时执行 15% 小幅复位，候选位于安全区域时不滚动。
- 单元回归已验证 `邮件_清理` 调用 `scroll_shape_content(list_shape)` 时不再传局部滚动覆盖参数。
- `2026-06-23 03:10` 扫描 `backend/core/fanxiu/data_annotation`、`backend/tests`、`tests`：业务任务调用 `scroll_shape_content(...)` 均未传局部 `ratio/duration/settle_seconds` 覆盖；剩余 `_drag_frame_point` 命中只属于测试、`debug_eval.drag()` 或 Runtime 底层实现，不属于滚动窗口业务残留。
- 仍需用一个真实滚动窗口验收：默认滚动、OCR 搜索、候选点击前截图/OCR 证据完整。当前真实画面停在 #265，#265「返回」点击前匹配未过，暂不为滚动验收硬点返回。

## 阶段 2：点击与场景切换等待基础设施

问题背景：

- #264 原先漏了场景标识时，理论上不应强行做源场景约束，只应短暂稳定等待后继续。
- 如果 shape 或调用方声明了目标场景，点击后应自动等待目标场景出现。
- 当前 `wait_click`、`go_scene`、普通点击和转场点击的语义需要更清晰。

待办：

- [x] 梳理 `wait_click`、`goto_view`、`go_scene`、scene jump target 的调用链：`wait_click` 负责源场景等待和点击，`go_scene` 负责 BFS 路径规划，局部转场点击不应混进两者任一极端。
- [x] 明确源场景等待规则：`wait_click` 当前在有 scene identity 时 wait 源场景；无源场景标识时跳过强场景等待。后续若要补“短暂 settle”，应作为局部转场 helper 的稳定等待，不改变所有 `wait_click` 默认语义。
- [x] 明确目标场景等待规则：新增 `FanxiuRuntime.wait_click_then_view(source, shape, *targets)`；调用方可显式传目标场景，也可从 shape 的 `sceneJumpTarget` 推断目标，缺少目标时失败并提示补标或显式声明。
- [x] 对目标未到达、到达未声明落点、场景 unknown 等情况中断并报告缺标注/错标注；`wait_click_then_view()` 目标等待超时时会保留 `wait_view` 的 unknown 诊断，并补充源场景、shape、期望目标和 `sceneJumpTarget`。
- [x] 保留 `go_scene` 的路径规划语义；新增的是局部 `wait_click_then_view` wrapper，没有把所有 `wait_click` 都变成强转场。

验收：

- 单元回归已验证 `wait_click_then_view()` 能从 `sceneJumpTarget=264` 推断目标并调用 `wait_view(264)`。
- 单元回归已验证缺少显式目标且无 `sceneJumpTarget` 时会失败，不会静默点击。
- 单元回归已验证目标场景等待超时时，错误信息包含源场景、shape、期望目标、`sceneJumpTarget` 和底层 unknown 诊断。
- `2026-06-23 03:00` 使用公开 `debug_eval` 手动作业验证 #265「返回」-> #264 的真实局部转场：Runtime 已等待并识别到 #265，但 #265「返回」shape 的图像匹配只有 `79%`，底层 `wait_click` 超时，没有执行点击；当前不硬点绕过，需人工确认 #265「返回」标注或匹配参数后再复验。
- `debug_eval` 稳定门面已补 `ctx.wait_click_then_view(...)`，后续可直接通过公开 Runtime 调试入口验证局部转场 helper。
- `debug_eval` 稳定门面已补只读 `ctx.shape_score(...)` 和 `ctx.shape_probe(...)`。`2026-06-23 03:09` 使用真实当前帧复查 #265：场景识别 `#265 100%`，`返回` shape 配置为 `imageMatchRole=required`、`ocrMatchRole=off`、`pixelTolerance=5`、`sceneJumpTarget=""`，image condition 相似度 `79%`，点击前实际门槛 `scene_threshold=80`，因此 `matched=false`；OCR 包含 `法则之主?/历史记录/魔道法则之主/剩余次数：0/1/已拜谒/天雷竹*1100`。这确认当前卡点是 #265「返回」标注或匹配参数，不应在基础设施里降级为硬点。
- `2026-06-23 03:13` 使用真实当前帧只读试算 `ctx.shape_probe(..., overrides=...)`：原始 `pixelTolerance=5` 为 `79%/matched=false`；覆盖为 `6` 时 `83%/matched=true`；覆盖为 `10` 时 `89%/matched=true`。该结果只作为人工修标候选，资产树未自动修改。
- 用真实 Runtime 验证至少一个“无源场景标识但可点击继续”的路径。
- 用真实 Runtime 验证至少一个“点击后等待目标场景”的路径。
- 失败时日志能指出缺哪个标注或哪个场景识别失败。

## 阶段 3：OCR 精细定位基础设施

问题背景：

- PaddleOCR 3.4.0 支持 `return_word_box`。
- 当前 CodeYun OCR 默认只消费 line 级结果，且 `_extract_ocr_line_entries()` 会按 y 归并，容易把相邻短标签拼成 `魔道仙弈`。
- 拜谒 #265 需要识别 `魔道` 后点击左侧图标，不能点合并文本框中心。

待办：

- [x] 给 OCR 服务的 per-call options 透传 `return_word_box`、`text_det_thresh`、`text_det_box_thresh`、`text_det_unclip_ratio` 等安全白名单参数。
- [x] 保持原有 `lines` 返回结构兼容，额外挂 `words` 或 `word_boxes`。
- [x] Runtime OCR helper 支持按需开启 `detail=word` 或 `return_word_box=true`。
- [x] 在匹配子串时优先使用 word/char box；没有 word box 时再回退到现有按字符串长度估算。
- [x] 避免全局开启 word box；只在需要精细点击的 shape/调用中开启。

验收：

- 单测验证 `return_word_box=True` 等 options 只按白名单透传，旧 `lines` 返回保持兼容。
- 单测验证 Runtime `ocr_words_in_shapes()` 会启用 `return_word_box` 并回填裁剪坐标。
- 真实 #265 识别区已命中 `魔道`，日志：`01:50:55 action: 日常_拜谒：OCR 命中「魔道」(魔道)，点击左侧 (787.9,768.5)`。
- `2026-06-23 02:44` 真实 probe 再次从 #34 进入 #265，在不点击选择目标的模式下 OCR 命中 `魔道`：词框 `(844.0,746.0,56.0,39.0)`，计算左侧图标区落点 `(790.8,763.5)`，随后点击 #265 `返回` 并通过 #264 OCR `千大道拜谒排行...16跨淡则...` 确认回到三千大道。
- 后续实测发现“固定左偏”不是通用规律：边缘 OCR 会产生负坐标，中心文字会误识别为 `魔道法则`。当前规则已改为：过滤含 `法则` 的规则说明文本；异常宽 OCR 框按右侧锚定拆出 `魔道` 子串；最终点击点从法则名词框推算到上方图标中心。
- 旧的 line 级 OCR 调用不需要改调用方也能继续工作。

## 阶段 4：拜谒功能闭环

当前标注事实：

- #264 位于 `/日常/拜谒/拜谒`，识别区用于滚动查找 `16跨法则`。
- #265 是 `法则之主`，识别区是旋转区域；目标是查找 `魔道`。
- #265 的 `魔道` 是动态旋转出现的，等待上限 120 秒；找不到应点返回，本次失败，下次重试。
- 点击目标应是 `魔道` 左侧图标区域，不是 OCR 文本中心。

待办：

- [x] 从 #264 使用默认滚动逻辑查找包含 `16` 的条目，并点击进入 #265。
- [x] 进入 #265 后等待场景稳定，确认顶部 `法则之主` 场景标识。
- [x] 在 #265 识别区开启 OCR word box，循环查找 `魔道`，上限 120 秒。
- [x] 命中 `魔道` 后使用 word/char box 计算点击点；当前计算已改为从词框推到左侧图标区，不再落在 OCR 文本框内部。
- [x] 点击后截图/OCR 验证是否进入拜谒确认、奖励、或已选择状态。
- [x] 若 120 秒未找到 `魔道`，点击 #265 `返回` 回 #264，记录本次失败并允许下次重试。

完成事实：

- 真实运行已从 #69 找到 `进行一次拜谒`，进入 #264，点击 `16跨法则`，进入 #265。
- 为避免继续误点，已新增显式 probe 模式：`{"baiye_lord_probe_only": true, "baiye_lord_probe_return": true}`。该模式可以真实进入 #265 并验证 OCR 命中，但不会点击选择 `魔道`，默认验证后返回 #264。
- `2026-06-23 02:44` probe 已验证 #265 动态旋转页能 OCR 查到 `魔道`，并记录词框和新左侧落点；该次没有执行选择点击，因此不能证明拜谒业务完成。
- `2026-06-23 02:47` 使用左偏模型点击 `剑魔道` 宽框时落点 `(229,763)` 仍未闭环，说明异常宽框不能按整框左侧估算。
- `2026-06-23 02:48` 使用左偏模型点击左边缘 `魔道仙弈` 时产生负坐标 `(-40,982)`，确认“点左边”不是通用规则。
- `2026-06-23 02:50` 使用上方图标模型但未过滤规则文本时误点 `魔道法则` 中心说明，仍未闭环。
- `2026-06-23 02:51` 过滤 `法则` 文本并点击外圈 `仙弈魔道` 的图标估算点 `(758,469)` 后，页面 OCR 出现完成态：`历史记录 魔道法则之主 ... 剩余次数：0/1 已拜谒 本次拜谒可获得：天雷竹*1100`。
- `2026-06-23 02:54` 再次运行 `daily_baiye`，当前完成态短路成功：`日常_拜谒完成`；Scheduler 写回 `legacy-daily-baiye last_result=success`、`last_run_at=2026-06-23 02:54:07`。
- `2026-06-23 02:59` 只读 `debug_eval` 在当前 #265 完成态 OCR 到 `魔道法则之主`、`剩余次数：0/1`、`已拜谒`、`天雷竹*1100`；该帧场景识别随后恢复为 #265 100%。
- `2026-06-23 03:17` 已把只读 `ctx.ocr_words_in_shapes(...)` 暴露给 `debug_eval`，用于后续直接验证 word-box OCR；当前完成态下 #265 标注 `识别区` 返回 `word_count=0`，这是因为已完成详情文本不在旋转选择识别区内，不能用于否定 02:44 动态页 probe。全帧只读 OCR 仍命中 `魔道法则之主`，并命中 `剩余次数：0/1`、`已拜谒`。

后续接手快照：

- 入口不是 `供奉`，而是 `日常/拜谒`；#69 日常滚动窗口中目标任务块包含 `进行一次拜谒`。
- #264 是三千大道页，需要在用户标注的识别区内用默认滚动查找包含 `16` 的 `16跨法则`，点击后进入 #265。
- #265 是 `法则之主` 旋转选择页，用户标注的识别区用于循环 OCR 查找 `魔道`；`魔道` 和 `仙弈` 应按词/字级 box 分开处理，不能点击合并 line 框中心。
- 命中 `魔道` 后点击点应来自目标词框对应的外圈图标，而不是固定点文本左侧。当前实现采用“词框上方图标中心”估算，并排除 `魔道法则/跨法则` 这类中心说明文本。
- 真实动作继续前先跑 `uv run python scripts/fanxiu_bt.py doctor --json`；若 Runtime、owner、manual_jobs 或隔离锁非空闲，不启动拜谒实验。
- 真实验收必须记录 Runtime action 日志、当前帧 OCR、点击点、点击后截图/OCR 和最终 Scheduler/Runtime 状态；单元测试只能证明接口层。

验收：

- 真实 Runtime 日志显示从 #264 进入 #265。
- 真实 #265 OCR 命中 `魔道`，并记录命中的原始文本、word box、计算点击点。
- 点击点来自 `魔道` 的词/字级 box，不再使用 `魔道仙弈` 合并框中心。
- `probe_only` 路径已实测不会点击选择目标，命中后返回 #264，可用于后续只读复验 OCR。
- 120 秒失败路径已实测会点击 #265 `返回`，返回动作有效；返回等待条件已改用 #264 语义 OCR。
- 真实完成态已验收：`daily_baiye` 最终 success，OCR 包含 `剩余次数：0/1 已拜谒`，Scheduler 写回成功。

## 阶段 5：复盘与文档回填

待办：

- [x] 把通用接口语义回填到长期约定文档，而不是只留在本任务清单。默认滚动已写入 `凡修行为树业务能力约定.md` 和 Runtime/Scheduler 文档；局部点击转场 `wait_click_then_view()` 已写入 `凡修行为树运行框架约定.md`；OCR word box/精细点击已写入业务能力与 Runtime/Scheduler 文档。
- [x] 若发现前端 UI 问题本质来自数据结构或业务建模不合理，记录到代码瘦身/重构自动化可消费的文档空间；已追加 `docs/CodeYun自动化协作交接.md` 的 `UI-HANDOFF-20260623-002`，并同步 `docs/CodeYun代码健康长期优化上下文.md`。
- [x] 删除或改写仍把“步进器”当主框架的残留描述；当前主框架是行为树。`docs/凡修data-annotation自动化Runtime与Scheduler.md` 已改为“旧前端长流程任务状态机/旧任务执行日志接口”，不再要求后续 AI 理解 stepper 框架。
- [x] 给后续 agent 留下当前状态、命令、证据路径和未完成项；见阶段 4 的“后续接手快照”和阶段 0/4 证据。

验收：

- 长期文档只记录稳定接口和原则，不混入一次性探针过程。
- 本清单中的已完成项有对应命令输出、日志或截图证据。
- `rg -n "步进器|stepper|Stepper" docs -S` 只剩本清单自身记录；正式长期文档不再把 stepper 当主框架描述。
- `rg -n "默认滚动|wait_click_then_view|ocr_words_in_shapes|return_word_box" docs/凡修行为树业务能力约定.md docs/凡修行为树运行框架约定.md docs/凡修data-annotation自动化Runtime与Scheduler.md -S` 能命中三类长期接口语义。
- `2026-06-23 03:19` 复跑上述两个 `rg` 门禁：`stepper/步进器` 只剩本清单自身记录；长期文档命中默认滚动、`wait_click_then_view`、`ocr_words_in_shapes` 和 `return_word_box`。

## 阶段 6：当前剩余阻塞与回归风险

当前阻塞：

- [ ] `2026-06-23 11:00` 心跳保底没有满足“到期作业清空后再维护”的前置条件。当前已知 `日常_灵塔` 成功，但 `日常_双修` 场景识别失败、`日常_报名` #86 -> #34 转场缺 `sceneJumpTarget` 声明，且仍有到期任务未执行；因此本轮不继续滚动/点击/OCR 基础设施维护和拜谒复验。
- [ ] #265「返回」需要人工修标或确认参数后再复验。只读证据显示原 `pixelTolerance=5` 为 `79%/matched=false`，临时覆盖为 `6` 时 `83%/matched=true`，但 Runtime/agent 不自动改资产树、不硬点返回。
- [ ] 阶段 1 的真实滚动窗口验收仍缺一条“默认滚动 + 识别区 OCR + 候选复核”的真实运行证据。当前卡在 #265 完成态，不能为了验收滚动而绕过 #265「返回」匹配。
- [ ] 阶段 2 的真实局部转场验收仍缺一条成功链路。#265「返回」-> #264 是最贴近当前任务的候选，但被同一个 #265「返回」匹配问题阻塞。
- [ ] 拜谒 OCR 后续只剩专项完善和复验：在真正处于 #265 旋转选择态时，用 `ocr_words_in_shapes(..., options={"return_word_box": True})` 证明能查到 `魔道`，并记录词框与推导点击点；已完成态全帧 OCR 命中 `魔道法则之主` 不能替代这条动态页证据。
- [ ] 当前推进条件：等待时间进入下一批日常到期窗口，或人工确认/修正 #265「返回」标注参数后再恢复真实验收。连续多轮保底巡检均为 `due=0`，因此当前没有可提交的业务作业。

回归备注：

- `2026-06-23 03:20` 运行宽回归 `uv run pytest backend/tests/test_fanxiu_data_annotation_runtime_guard.py tests/test_fanxiu_behavior_tree_local.py tests/test_fanxiu_data_annotation_scheduler.py -q`：`627 passed / 61 failed`。失败集中在更大范围的日常任务迁移和旧测试预期，例如默认日常任务 `enabled` 已为 `true` 而旧断言期望 `false`，以及双修、灵祖、异火、仙缘等任务的旧构造测试与当前实现不一致。
- 同时复跑本轮窄回归 `test_debug_eval_context_exposes_ocr_words_in_shapes`、`test_debug_eval_context_shape_probe_reports_score_and_shape_metadata`、`test_debug_eval_context_exposes_wait_click_then_view`：`3 passed`。因此当前新增 debug_eval 探针能力本身通过，但不能据此宣称全量凡修回归通过。
- 宽回归会污染本机 Runtime/Scheduler 状态：本轮误写了 `daily-boss` 与 `legacy-daily-signup` 的 `last_result=success`，并写入一条未来时间的 `legacy-daily-yaowang` 测试日志。已备份并清理：
  - 备份目录：`C:\Users\kzche\AppData\Local\Temp\codeyun\fanxiu_state_restore_20260623_0323`
  - 已恢复 `scheduler_tasks.json` 中 `daily-boss`、`legacy-daily-signup` 的 `last_run_at=null`、`last_result=""`、`checkpoint=null`
  - 已删除 `world_facts.json` 中 `daily-boss`、`legacy-daily-signup` 的测试发现项
  - 已删除 `runtime_state.json` 中 `06:00 legacy-daily-yaowang` 与 `Scheduler 任务已停止` 的测试日志
  - `2026-06-23 03:24` 复查 `doctor --json`：`due_tasks=[]`，Runtime/owner/manual_jobs/隔离锁均空闲，相关日志为空。
