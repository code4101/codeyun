# ==========================================
# Legacy Configuration (Deprecated)
# ==========================================
# 服务器连接信息 (SSH) - 仅供旧脚本使用
SSH_HOST=1.1.1.1
SSH_USER=ubuntu
SSH_PASSWORD=your_secure_password
SSH_PORT=22

# ==========================================
# New Standard Configuration (Use these in new code)
# ==========================================
# Server IP
YUN_SERVER_HOST=43.161.220.95

# Regular User (Recommended)
# 用于常规操作、应用部署、日志查看等
YUN_USER_CHENKUNZE=chenkunze
YUN_USER_PASS_CHENKUNZE=change-me-to-your-chenkunze-password

# Admin User (Use with caution)
# 仅在普通用户权限不足时使用
YUN_USER_UBUNTU=ubuntu
YUN_USER_PASS_UBUNTU=change-me-to-your-ubuntu-password

# ==========================================
# Other Configuration
# ==========================================
# DSP 计算器源码路径 (用于 build_dsp_static.py)
DSP_SOURCE_DIR=D:\path\to\dsp-calc

# 应用密钥
# 生成安全密钥 (例如执行 `openssl rand -hex 32`)
CODEYUN_SECRET_KEY=change-me-to-a-secure-random-string

# 数据库
# 简单部署使用 sqlite，或者配置 postgresql://...
DATABASE_URL=sqlite:///backend/data/codeyun.db

# 其他配置
# LOG_LEVEL=INFO
